{
  "name": "battrochtek",
  "version": "36.15.0",
  "private": true,
  "description": "Drum machine et bibliothèque de grooves PWA basée sur la Web Audio API.",
  "scripts": {
    "postinstall": "node scripts/vendor-alpine.mjs && node scripts/vendor-oat.mjs",
    "dev": "http-server . -p 8000 -c-1",
    "start": "http-server . -p 8000 -c-1",
    "lint": "eslint app.js service-worker.js scripts/*.mjs eslint.config.js",
    "format": "prettier --write \"*.{js,json,md,html,css}\" \"scripts/**/*.mjs\"",
    "format:check": "prettier --check \"*.{js,json,md,html,css}\" \"scripts/**/*.mjs\"",
    "test": "node scripts/smoke.mjs",
    "check": "node --check app.js && node --check service-worker.js && npm run lint && npm test",
    "build": "node scripts/build-pages.mjs",
    "grooves:sync": "node scripts/sync-grooves.mjs",
    "grooves:clean": "node scripts/clean-grooves.mjs",
    "grooves:build": "node scripts/clean-grooves.mjs"
  },
  "dependencies": {
    "alpinejs": "3.15.12",
    "@knadh/oat": "^0.4.1"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "http-server": "^14.1.1",
    "prettier": "^3.0.0"
  },
  "engines": {
    "node": ">=24"
  },
  "type": "module"
}
