/*
 * Alpine.js is vendored here by `npm install` via scripts/vendor-alpine.mjs.
 * This placeholder intentionally provides no fallback implementation.
 * Run `npm install` before starting Battrochtek.
 */
